{
  "name": "@hegpetac/music-collab-playlist-api",
  "version": "1.1.21",
  "description": "OpenAPI specification for Collaborative music playlist handler frontend",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/main/resources dist",
    "lint": "redocly lint src/main/resources/music-collab-playlist-api.yaml"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "dependencies": {
    "@redocly/cli": "^2.11.1"
  }
}
